<dec f='include/google/protobuf/extension_set.h' l='589' type='std::map&lt;int, Extension&gt;'/>
<offset>0</offset>
<doc f='include/google/protobuf/extension_set.h' l='583'>// The Extension struct is small enough to be passed by value, so we use it
  // directly as the value type in the map rather than use pointers.  We use
  // a map rather than hash_map here because we expect most ExtensionSets will
  // only contain a small number of extensions whereas hash_map is optimized
  // for 100 elements or more.  Also, we want AppendToList() to order fields
  // by field number.</doc>
